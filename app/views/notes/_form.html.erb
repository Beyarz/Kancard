<%= form_with model: [@board, @board.notes.build] do |f| %>
  <div id="new-note">
    <div class="field">
      <%= f.label :content, :class => "label" %>
      <div class="control has-icons-left">
        <%= f.text_field :content, :class => "input", :placeholder => "Content", :aria_required => true, required: true %>
        <span class="icon is-small is-left">
          <i class="fas fa-align-left"></i>
        </span>
      </div>
    </div>
  </div>

  <div class="field is-grouped is-grouped-centered">
    <div class="control" style="padding-top: 1rem;">
      <button type="reset" class="button bold" id="note-back-button">Back</button>
      <%= f.submit "Create", :class => "button is-custom-blue" %>
    </div>
  </div>
<% end %>
